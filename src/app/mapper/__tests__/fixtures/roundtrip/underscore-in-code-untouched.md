Run `foo\_bar` to see it, or use the pattern in the block below. A code span can also
be delimited by double backticks so it can contain a literal backtick, like
``foo`bar\_baz``.

```python
import re
pattern = re.compile(r"foo\_bar")
```
