{
  "name": "liminis-editor-electron-shell",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Minimal Electron host for @liminis/editor — a window, the editor, and stubbed host services — used to run package-level e2e against real Chromium/Electron (verveguy/liminis-editor#2). Not a liminis-app replacement.",
  "main": "main.cjs",
  "scripts": {
    "build": "vite build",
    "test:e2e": "playwright test --config e2e/playwright.config.ts"
  },
  "dependencies": {
    "react": "^19.2.5",
    "react-dom": "^19.2.5"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@vitejs/plugin-react": "^6.0.1",
    "electron": "^41.3.0",
    "vite": "^8.0.10"
  }
}
